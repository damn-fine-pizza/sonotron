function(arrangrr_test name)
  add_executable(${name} ${name}.cpp)
  target_link_libraries(${name} PRIVATE arrangrr_core)
  add_test(NAME ${name} COMMAND ${name})
endfunction()

arrangrr_test(test_common)
arrangrr_test(test_midi)
arrangrr_test(test_engine)
arrangrr_test(test_engine_fire_order)
arrangrr_test(test_timeline)
arrangrr_test(test_step_locks)
arrangrr_test(test_chord)
arrangrr_test(test_chord_seq)
arrangrr_test(test_chord_detect)
arrangrr_test(test_arp)
arrangrr_test(test_arranger)
arrangrr_test(test_scheduler)
arrangrr_test(test_gesture)
arrangrr_test(test_voicing)

arrangrr_test(test_host)
target_link_libraries(test_host PRIVATE arrangrr_host)

arrangrr_test(test_panels)
target_link_libraries(test_panels PRIVATE arrangrr_host)

arrangrr_test(test_panel_nav)
target_link_libraries(test_panel_nav PRIVATE arrangrr_host)

arrangrr_test(test_style_chooser)
target_link_libraries(test_style_chooser PRIVATE arrangrr_host)

arrangrr_test(test_rc_config)
target_link_libraries(test_rc_config PRIVATE arrangrr_host)
