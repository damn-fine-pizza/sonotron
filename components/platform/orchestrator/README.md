HOST-ONLY. Names and composes the concrete multi-stage pipelines (Accompany,
roadmap node 9310) out of `components/runtime`'s dual-target
`Pipeline<StageTs...>` mechanism plus `components/midisrc`'s host-only
MIDI-source stage. See `include/orchestrator/accompany.hpp` and
`docs/design/orchestrator-pipeline-extraction.md` §16.3/§16.5/§16.8.
